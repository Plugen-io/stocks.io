/**
 * Entry point do servidor mTLS.
 * Implementação completa: Fase 2 (mTLS skeleton) + Fase 3 (endpoints).
 *
 * Por enquanto, só placeholder.
 */
console.log('Stock.io server — placeholder. Rode `npm run ca:bootstrap` antes do dev.');
